#include <stdio.h>
#include <stdlib.h>
//WAP to display all even numbers from 1 to n.
int main()
{
    int number=2, n;
    printf("Enter a number:");
    scanf("%d", &n);
    while(number<=n)
    {
        printf("%d\n", number);
        number+=2;
    }
}
