#include <stdio.h>
#include <stdlib.h>
//WAP to print all numbers that are divisible by 3 and 5 in the range 1 to 1000.
int main()
{
    int number;
    number=15;
    while(number<=1000)
    {
        printf("%d\n", number);
        number+=15;
    }
}
